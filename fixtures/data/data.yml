Person:
 person_{1..10}:
   name: "{{name.findName}}"
   email: "{{internet.email}}"

Attendance:
  attendance_{1..100}:
    startDate: "{{date.past}}"
    endDate: "{{date.past}}"
    personId: @person.*
